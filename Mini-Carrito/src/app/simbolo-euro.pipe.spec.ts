import { SimboloEuroPipe } from './simbolo-euro.pipe';

describe('SimboloEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new SimboloEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
